---
description: How to build the ScreenSense project
---
This workflow describes how to install dependencies and build the ScreenSense Next.js application.

1. Ensure you have the required Node.js version installed (>= v20).
2. Install npm dependencies:
```bash
npm install
```
3. Compile and build the Next.js application:
```bash
npm run build
```
